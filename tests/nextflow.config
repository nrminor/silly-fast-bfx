// nf-test requires a test config file. The selected `test` profile in the
// repository nextflow.config owns executor limits and process resources.

process.beforeScript = "export PATH='${projectDir}/tests/bin':\$PATH"
